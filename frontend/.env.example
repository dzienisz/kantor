# Skopiuj do .env.local (lokalnie) lub ustaw w panelu hostingu (Vercel/Cloudflare Pages).
#
# Adres backendu API. Puste = ten sam origin (uzywany dev-proxy Vite na /api).
# Produkcja (frontend na innej domenie niz backend): podaj pelny URL backendu, np.:
# VITE_API_URL=https://kantor-backend.up.railway.app
VITE_API_URL=
